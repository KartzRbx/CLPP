-- Compiled by CL++ — C++ × Luau

const function PlayerData()
	return {
		Currencies = { Coins = 0, Rebirths = 0 },
		Inventory = { HotBar = {}, Storage = {}, MaxStorage = 90 },
	}
end

return PlayerData

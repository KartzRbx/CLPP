#pragma once

struct DataServiceServer {
    Data WaitFor(Player player);
};

struct DataService {
    DataServiceServer Server;
    any Paths;
};

struct Data {
    RBXScriptSignal GetChangedSignal(any path);
    void Destroy();
};

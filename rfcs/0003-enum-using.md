# S5 enum and using

`enum class` emits a frozen string table plus a Luau string union. `enum : int` is numeric. `using Name = Type` emits `type Name = Type`. `namespace` stays CLPP0301 until modules exist.

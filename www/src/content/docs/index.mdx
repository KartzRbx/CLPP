---
title: CL++
description: A C++-inspired language that compiles to Luau. User guide, language reference, and specification.
template: splash
hero:
  title: CL++
  tagline: A C++-inspired language that compiles to Luau. Syntax, semantics, and OOP — not a Roblox SDK.
  actions:
    - text: Install
      link: docs/install
      icon: rocket
      variant: primary
    - text: Hello world
      link: docs/hello-world
      icon: right-arrow
    - text: Language reference
      link: docs/reference
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";

<CardGrid stagger>
  <Card title="User guide" icon="open-book">
    Install the compiler, write a first script, then learn types, operators, `@this`, and style.
    Start at [What is CL++?](docs/intro).
  </Card>
  <Card title="Language reference" icon="list-format">
    One page per construct: syntax, parameters, Luau emit, example. Same job as a C++ reference.
    Open the [index](docs/reference).
  </Card>
  <Card title="Specification" icon="seti:json">
    Pest EBNF, type table, and how `clpp` emits Luau. For contributors and host tools.
    See the [compiler pipeline](docs/spec/compiler).
  </Card>
</CardGrid>

## How the compiler sits in a project

<pre class="mermaid">
flowchart LR
  src[".clpp / .clh / .clp"] --> pest["Pest grammar"]
  pest --> check["Semantic check"]
  check --> luau["Luau emit"]
  luau --> rojo["Rojo / Cluaupp"]
  src --> lsp["Editor pack + LSP"]
</pre>

CL++ is the **language**. [Cluaupp](https://github.com/KartzRbx/Cluaupp) owns engine headers, Rojo, and project `init`. There is no bytecode VM and no C++ hosting API — `clpp` writes Luau files.

## Two accessors to remember

| You write | You mean |
| --- | --- |
| `player.Name` / `player.Kick()` | property / instance method |
| `@this` / `@janitor` | receiver and field inside `Class::Method` |
| `players.PlayerAdded~>Connect` | Janitor connection |
| `Player player` | Instance of class Player |
